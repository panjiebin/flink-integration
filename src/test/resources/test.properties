# ===========================================================================
# Configure Kafka
# ===========================================================================
kafka.bootstrap.servers = 127.0.0.1:9092
kafka.group.id= test
kafka.topic.test = test_topic
kafka.topic.test.partition = 5
kafka.topic.alarm = alarm_topic
kafka.topic.alarm.partition = 2

# ===========================================================================
# Configure watermark
# ===========================================================================
# watermark delay time(min)
watermark.delay.time = 1
